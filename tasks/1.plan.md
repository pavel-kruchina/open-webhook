# Plan — Task 1: Rebrand to "open-webhook" (proper fork)

## Approach
Change only the **product brand** ("WebHook Tester" / "Webhook Tester" → "open-webhook") and the repo link.
Leave the generic word "webhook"/"WebHook" (meaning a webhook endpoint/session) untouched — e.g. "A new WebHook
has been created" must NOT become "a new open-webhook". Keep the Go module path and internal storage keys.

## Subtasks

### 1. Frontend — logo → text (top-left)
- `web/src/screens/components/header/header.tsx`: replace `<Image src={LogoTextSvg}>` with a text mark
  "open-webhook" (Mantine Text/Title, keep version tooltip). Remove the `logo-text.svg` import.

### 2. Frontend — repo link near Help
- `web/vite.config.ts`: `__GITHUB_PROJECT_LINK__` → `https://github.com/pavel-kruchina/open-webhook`;
  `__LATEST_RELEASE_LINK__` → `.../releases/latest`.

### 3. Frontend — product-name strings & metadata
- `web/index.html`: `<title>` and `apple-mobile-web-app-title` → `open-webhook`.
- `web/public/site.webmanifest`: `name` / `short_name` → `open-webhook`.
- `web/src/screens/home/screen.tsx` (intro line): "WebHook Tester allows…" → "open-webhook allows…".
- `web/src/screens/components/header/components/help-modal.tsx`: "What is Webhook Tester?" / "Webhook Tester lets you…"
  → "open-webhook".
- `web/src/screens/components/header/components/new-session-modal.tsx`: default `Server: WebhookTester` header value
  → `Server: open-webhook` (placeholder + default).
- Do NOT touch the many generic "WebHook" notification strings (they mean a webhook, not the brand).

### 4. Backend CLI brand string
- `internal/cli/app.go`: `Usage: "webhook tester"` → `Usage: "open-webhook"`; regenerate README CLI-docs block.

### 5. Docs / info files
- `README.md`: title → `# open-webhook`; add a prominent "Fork of tarampampam/webhook-tester" attribution near the
  top (keep upstream credit); point the header link/badge at the fork; **remove** the two AI sections
  ("A note on AI-assisted development", "AI Agent Instructions"); verify feature claims (storage = Redis-only —
  already done; multipart files section — already present; re-scan for any other stale claims). Keep upstream
  release/image/demo references as upstream's (reinforces the fork relationship).
- `AGENTS.md`: project name line → "open-webhook (a fork of webhook-tester)"; keep module path note.
- `deployments/helm/Chart.yaml`: `home`/`sources` → fork repo; keep chart identifier `name` and the upstream image.

## Files to touch
header.tsx, vite.config.ts, index.html, site.webmanifest, home/screen.tsx, help-modal.tsx, new-session-modal.tsx,
internal/cli/app.go (+ regen README), README.md, AGENTS.md, deployments/helm/Chart.yaml

## Out of scope
Go module path, internal Dexie DB name / storage-key prefix, Helm template helper identifiers, CI binary names,
upstream distribution URLs (kept for attribution). No logic changes.

## Verification
- Web gate: `npm --prefix ./web run lint && build && test`.
- Go gate: `go build ./... && go test ./...` (after app.go change + README regen).
- Live: open the UI, confirm top-left shows "open-webhook" text, tab title is "open-webhook", and the GitHub
  button points to the fork.
- README: AI sections gone, fork attribution present, no claims of memory/fs storage, files feature documented.
